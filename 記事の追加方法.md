# 記事の追加方法

1. `templates/article.md` を `src/content/blog/` にコピーします。
2. ファイル名を英小文字とハイフンで付けます。例：`cat-care.md` → `/blog/cat-care/` というURLになります。
3. 冒頭の `title`（タイトル）・`publishDate`（日付）と、その下の本文を書き換えます。日付は `"2026-09-23"` のように引用符で囲みます。
4. 公開するときは `draft: true` を `draft: false` に変えます。
5. ビルドを確認してGitHubのmainブランチへ送信すると、Cloudflareが公開します。

記事のデザイン・肉球ファビコン・ヘッダー・フッターは自動で付きます。トップページの記事一覧にも日付の新しい順で追加されます。下書きは一覧にも公開用ページにも出ません。日付を未来にしても予約投稿にはならないので、未公開の記事には `draft: true` を指定してください。

## 本文の書き方

`## 見出し` で見出し、`- 項目` で箇条書きになります。段落は空行で区切ります。デザインのコードをコピーする必要はありません。

## 必要な場合だけ追加する項目

冒頭の2つの `---` の間に追加します。写真がなくても記事を作れます。

```yaml
description: "検索結果などで使う記事の説明"
subtitle: "タイトルの下に表示する短い説明"
category: "猫の健康"
image:
  src: "/images/cat-care.jpg"
  alt: "写真の内容を説明する文章"
  width: 1600
  height: 900
```

写真は `public/images/` に入れ、幅と高さには画像の実際のピクセル数を書きます。

見出しを意図した位置で改行したい場合だけ、次の項目を追加します。

```yaml
titleLines:
  - "タイトルの1行目"
  - "タイトルの2行目"
```

既存記事の例：`src/content/blog/dog-vomiting.md`。
共通デザイン：`src/layouts/BlogLayout.astro` と `src/styles/global.css`。
